NTIMES := 500
STREAM_ARRAY_SIZE := 4096
TARGET := stream
GCC_OPTS += -O3 -flto
CFLAGS += -DSTREAM_TYPE=uint32_t
CFLAGS += -DSTREAM_ARRAY_SIZE=$(STREAM_ARRAY_SIZE)
CFLAGS += -DNTIMES=$(NTIMES)

all: $(TARGET).elf

include ../Makefile.inc
